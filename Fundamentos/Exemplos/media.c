#include <stdio.h>

int main() {
    // declaração de variáveis
    // entrada
    int nota1 = 8;
    int nota2 = 7;

    // processamento
    int media = (nota1 + nota2) / 2;
    
    // saída
    printf("A media final e %d", media);

    return 0; 
}
