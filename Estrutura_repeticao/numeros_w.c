#include <stdio.h>

int main() {
    int contador = 0;

    while (contador <= 10) {
        printf("\n numero %i", contador);
        contador = contador + 1; // contador++;

    }
     return 0;
    
}